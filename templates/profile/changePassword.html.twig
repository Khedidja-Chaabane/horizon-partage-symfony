{% extends 'base.html.twig' %}
{% block title %}Mise à jour du mot de passe
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('styles/user/forms.css') }}">
{% endblock %}
{% block body %}

	<div class="container my5">
		<div class="d-flex justify-content-end mb-4 mt-4">
			<a class="btn btn-secondary btn-retour" href="{{ path('app_profile') }}">
				<i class="bi bi-arrow-left-square"></i>
				Retour</a>
		</div>
		<h1 class="text-center mb-4 mt-4">
			Changer de mot de passe
		</h1>
		<div class="d-flex justify-content-center">
			<div class="col-lg-8 col-md-10">
				{{ form_start(form) }}
				{{ form_widget(form) }}
				<button class="btn btn-submit w-100 mt-4">Changer de mot de passe</button>
				{{ form_end(form) }}</div>
		</div>
	</div>

{% endblock %}
