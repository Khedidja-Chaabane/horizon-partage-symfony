{% extends 'base.html.twig' %}

{% block title %}Nouvelle info
{% endblock %}
{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('styles/forms.css') }}">
{% endblock %}

{% block body %}

	<div class="container my-5">
		<h1 class="text-center mt-5 mb-5">Nouvelle actualité</h1>
		<div class="d-flex justify-content-end mb-4 mt-4">
			<a class="btn btn-secondary btn-retour" href="{{ path('gestion_infos') }}">
				<i class="bi bi-arrow-left-square"></i>
				Retour</a>
		</div>
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-submit w-100 mt-3">Sauvegarder</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
